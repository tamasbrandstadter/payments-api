version: '3.5'

services:
  postgres:
    image: postgres:latest
    container_name: db
    hostname: db
    ports:
      - 5432:5432
    env_file:
      - ./postgres/database.env
    volumes:
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql